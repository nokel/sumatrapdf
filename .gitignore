*.suo
*.aps
*.opensdf
*.sdf
*.vspx
*.vcxproj.user
*.cov
out.txt
out/
deps/
agent-tools/
ext/mupdf/generated/
.vs/
.vscode/db/
.vscode/ipch/
# src/base/BuildConfig.h
artifacts/
.idea/
src/.vscode
tools/pdfjs/coverage/
cppcheck.out.txt
clangtidy.out.txt
node_modules

.claude/settings.local.json
pipe-preview-results
nul
docs/scratch.txt
bin/zopflipng.exe

cmd/tmp/

.work/
log.txt
sumlog.txt
vs2022/enc_temp_folder
docs/md/.obsidian

# temporary / scratch output written by tests (see tests/util.ts tmpPath())
tests/tmp/
# one-off GUI scripts written while working on a change, not part of the suite
tests/ad-hoc-*.ts
# screenshots taken while checking the UI
/lib*.png
# stale build output: translations now live in .work/
translations/
/.tmp
/terminals
/mcps/tasks/tools
/bin/resvg.exe
# agents.md is maintained per branch locally, so it can describe that branch
/agents.md

# android build output and machine-local gradle state
android/app/build/
android/.gradle/
android/local.properties
*.apk
*.aab

# agent scratch space: probes, snapshots and backups taken while working
/scratchpad/
# benchmark and one-off verification scripts, not part of the suite
tests/bench-*.ts
tests/verify-*.ts

# the old cmake build tree and install prefix of the OCR deps, now built from ext/a-leptonica and ext/a-tesseract
ext/build/
# OCR smoke tests, fixture generators and their output
ext/*.exe
ext/*.obj
ext/*.bat
ext/*.py
ext/*.cpp
ext/*.cmake
ext/ocr-test.pdf
# test suites, probes and fixtures written for this fork; upstream's own tests/ files stay tracked
/tests/*
# harness modules the tracked suites import, so a clean checkout can run them
!/tests/gui-watchdog.ts
/build-check/
# agent notes and dumps, not part of the program
/.agents/
/docs/AI-*.md
/docs/LIBRARY-NOTES.md
/docs/book-record.md
/menu-dump.txt
# extracted driver installers
/$extract/
/$extractDir/
# scratch build/run logs
tmp*.log
# python bytecode from the test scripts
__pycache__/
# editor/scratch leftovers
*.bak
*.removed
# per-book sidecars written by the app while testing
tests/*.sumatra
# QA screenshots taken while checking the UI
/qa-*.png
/src/tools/bench_library.cpp
